#include <stdio.h>

/* This is a comment */
int main (int argc, char *argv[])
{
    int distance = 100;

    int added_distance = 25;
    distance = distance + added_distance;

    // this is also a comment
    printf("You are %d miles away. \n", distance);

    return 0;
}
